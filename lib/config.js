/**
 * Configuration for running test
 * It will load the config.json to set up the run time environment when the server
 */
module.exports = {
    port: 3000,
    ui: "ui",
    template:"templates",
    resource:"resources"
}